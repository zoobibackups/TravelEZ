import React from 'react';
import {View} from 'react-native';

const getRoomAmenities = (array_data, item) => {
  let data = {item_name: 'tv', text: 'No Tv'};
  return <View style={{flexDirection: 'row'}}></View>;
};

export default getRoomAmenities;
